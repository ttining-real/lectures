// * Quiz 1
type Nums = number[];
type Colors = string[];
type Coords = [number, number][];
type Info = [number, string][];

// * Quiz 2
type Course = {
  name: string;
  price: number;
  student_cnt: number;
  author: string;
  related_courses: string[];
};

// * Quiz 3
type User = {
  name: string;
  email: string;
  [key: string]: string;
};
